import React from 'react';
import useTitle from '../../Hooks/useTitle';

const MyLeaflet = () => {
    useTitle('My Leaflet');
    return (
        <div>
            <h2>My Leaflet</h2>
        </div>
    );
};

export default MyLeaflet;